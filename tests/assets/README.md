# Deterministic fixtures

No binary fixtures have been added during scaffolding. The implementation should introduce tiny,
locally available, license-audited assets with recorded generation inputs and hashes.

Include a tiny valid image, representative image, wide/tall images, malformed and unsupported
payloads, Persian/English and bidirectional text, special filenames/captions, and deterministic
custom-emoji examples. Keep intentionally malformed assets clearly labeled and inert.

Custom emoji fixture identifiers belong to the simulated world; they are not claims about
production or Test-DC documents. Preserve fallback and unavailable-document cases.
