# Independent public rich-button scenario acceptance

Type: task
Status: ready-for-agent
Work state: open
Blocked by: frozen contract70; green execution waits for coordinator73 implementation

Own this ticket, new `tests/fixtures/rich_targets_bot.py`, new
`tests/test_runner_rich_targets.py` and optional new `tests/rich_targets_scenario.py` only.
No production, existing tests, native patches, lockfile or shared docs. Follow the
[frozen contract](../../../docs/development/rich-button-implementation-contract.md).

Author a compact typed real contained bot and the same public scenario for simulation/native.
Use independently authored complete canonical expected messages, observations, receipts/effects,
callback updates/history and bot output; do not derive expectations from the production traversal.
Cover original row and inline callback/copy/disabled placements, repeated labels/payloads,
Persian/English and nested canonical paths. Hidden/offscreen targets must remain observable and
reject explicitly where unavailable; this is not an automatic-scrolling implementation.

Use focused scenarios for same-clock/ABA and unrelated edits, first-call stale rejection and
repeat receipt without second action. Check full frozen callback snapshot and exact payload,
copy/disabled absence of operation-owned World events, and unchanged complete quiet state where
appropriate. Account for ordinary/rich shared64 slot reservations. Fixture deadlines must budget
native operations separately from short semantic polling waits; do not change product timeouts.
Native expectations additionally require original evidence and exact private correlation, with
actual clipboard paste reserved for the coordinator's native gate.

Useful work can proceed before73: assemble fixtures, compile/static-check them and retain the
actual contained public missing-operation red. Do not fake a successful Scenario method, patch
production to manufacture green, or repeatedly rerun before a dependency changes. Coordinator
will supply a reviewed frozen dependency for focused green checks using the documented loan tool
if needed. No guest/build/full gate. Finish with a frozen clean branch, meaningful red/green or
explicit unavailable evidence and terminal processes. Worker fixture completion alone does not
resolve native acceptance or the operational milestone.
