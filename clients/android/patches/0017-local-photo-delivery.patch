diff --git a/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabBridge.java b/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabBridge.java
index 5bd5d9e..11f71ba 100644
--- a/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabBridge.java
+++ b/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabBridge.java
@@ -60,7 +60,7 @@ public final class GramLabBridge {
         }
     }
 
-    private static void fields(JSONObject value, String... allowed) {
+    static void fields(JSONObject value, String... allowed) {
         Set<String> names = new HashSet<>(Arrays.asList(allowed));
         Iterator<String> keys = value.keys();
         while (keys.hasNext()) {
@@ -68,7 +68,7 @@ public final class GramLabBridge {
         }
     }
 
-    private static long identifier(JSONObject value, String key) throws JSONException {
+    static long identifier(JSONObject value, String key) throws JSONException {
         Object number = value.get(key);
         require(number instanceof Integer || number instanceof Long);
         long result = ((Number) number).longValue();
@@ -84,7 +84,7 @@ public final class GramLabBridge {
         return result;
     }
 
-    private static int integer(JSONObject value, String key) throws JSONException {
+    static int integer(JSONObject value, String key) throws JSONException {
         long result = nonnegative(value, key);
         require(result <= Integer.MAX_VALUE);
         return (int) result;
@@ -97,7 +97,12 @@ public final class GramLabBridge {
     }
 
     private static TLRPC.TL_message message(JSONObject record, long persona, long bot) throws JSONException {
-        fields(record, "id", "chat_id", "sender_id", "date", "text", "reply_markup", "edit_date", "entities", "rich_message");
+        fields(record, "id", "chat_id", "sender_id", "date", "text", "reply_markup", "edit_date", "entities", "rich_message", "photo", "caption", "caption_entities");
+        boolean hasPhoto = record.has("photo");
+        require(!record.has("caption") || hasPhoto);
+        require(!record.has("caption_entities") || hasPhoto);
+        require(!hasPhoto || !record.has("entities"));
+        require(!record.has("rich_message") || !hasPhoto && !record.has("caption") && !record.has("caption_entities"));
         long sender = identifier(record, "sender_id");
         require(sender == persona || sender == bot);
         TLRPC.TL_message message = new TLRPC.TL_message();
@@ -116,9 +121,26 @@ public final class GramLabBridge {
             require(sender == bot && message.message.isEmpty() && !record.has("entities"));
             message.rich_message = GramLabRichMessage.decode(record.getJSONObject("rich_message"));
             message.flags2 |= 1 << 13;
-        } else require(!message.message.isEmpty());
-        message.media = new TLRPC.TL_messageMediaEmpty();
+        } else require(record.has("photo") || !message.message.isEmpty());
+        if (record.has("photo")) {
+            require(sender == bot && message.message.isEmpty() && !record.has("rich_message"));
+            JSONObject photoRecord = record.getJSONObject("photo");
+            fields(photoRecord, "asset_id");
+            TLRPC.TL_messageMediaPhoto media = new TLRPC.TL_messageMediaPhoto();
+            media.photo = GramLabMedia.photo(identifier(photoRecord, "asset_id"), message.date);
+            media.flags |= 1;
+            message.media = media;
+            if (record.has("caption")) {
+                require(record.get("caption") instanceof String);
+                message.message = record.getString("caption");
+            }
+        } else message.media = new TLRPC.TL_messageMediaEmpty();
         message.flags = (1 << 8) | (1 << 9);
+        if (record.has("caption_entities")) {
+            require(record.has("photo") && !record.has("entities"));
+            message.entities = entities(record.getJSONArray("caption_entities"), message.message);
+            if (!message.entities.isEmpty()) message.flags |= 1 << 7;
+        }
         if (record.has("entities")) {
             message.entities = entities(record.getJSONArray("entities"), message.message);
             if (!message.entities.isEmpty()) message.flags |= 1 << 7;
@@ -238,7 +260,7 @@ public final class GramLabBridge {
     }
 
     private static JSONObject exchange(JSONObject configuration, String path, JSONObject parameters) throws Exception {
-        fields(configuration, "endpoint", "capability", "world_id", "user_id");
+        fields(configuration, "endpoint", "capability", "world_id", "user_id", "bridge_version");
         URL endpoint = new URL(configuration.getString("endpoint"));
         require("http".equals(endpoint.getProtocol())
                 && ("10.0.2.2".equals(endpoint.getHost()) || "127.0.0.1".equals(endpoint.getHost()))
@@ -288,19 +310,25 @@ public final class GramLabBridge {
         } finally {
             connection.disconnect();
         }
-        require(integer(body, "schema") == (path.startsWith("/v2/") ? 2 : 1) && worldId.equals(body.getString("world_id"))
+        int expectedSchema = path.startsWith("/v3/") ? 3 : path.startsWith("/v2/") ? 2 : 1;
+        require(integer(body, "schema") == expectedSchema && worldId.equals(body.getString("world_id"))
                 && identifier(body, "user_id") == persona);
         return body;
     }
 
     public static Snapshot connect(JSONObject configuration) throws Exception {
-        JSONObject body = exchange(configuration, "/v2/snapshot", null);
+        int version = configuration.has("bridge_version") ? integer(configuration, "bridge_version") : 2;
+        require(version == 2 || version == 3);
+        JSONObject body = exchange(configuration, version == 3 ? "/v3/snapshot" : "/v2/snapshot", null);
         String worldId = configuration.getString("world_id");
         long persona = identifier(configuration, "user_id");
         fields(body, "schema", "world_id", "user_id", "cursor", "now", "users", "chats", "messages",
-                "message_position", "sends");
-        require(integer(body, "schema") == 2 && worldId.equals(body.getString("world_id"))
+                "message_position", "sends", "assets", "message_revisions");
+        require(integer(body, "schema") == version && worldId.equals(body.getString("world_id"))
                 && identifier(body, "user_id") == persona);
+        GramLabMedia.beginWorld(configuration, worldId);
+        if (version == 3) { GramLabMedia.install(body.getJSONArray("assets")); body.getJSONArray("message_revisions"); }
+        else require(!body.has("assets") && !body.has("message_revisions"));
         long cursor = nonnegative(body, "cursor");
         int now = integer(body, "now");
         TLRPC.TL_messages_dialogs dialogs = new TLRPC.TL_messages_dialogs();
@@ -363,13 +391,22 @@ public final class GramLabBridge {
         }
         require(users.size() == peersByChat.size() + 1);
         records = body.getJSONArray("messages");
+        JSONArray revisions = version == 3 ? body.getJSONArray("message_revisions") : null;
+        if (revisions != null) require(revisions.length() == records.length());
         Map<Long, Set<Integer>> seen = new LinkedHashMap<>();
         for (Long chat : peersByChat.keySet()) {
             seen.put(chat, new HashSet<>());
         }
         for (int index = 0; index < records.length(); index++) {
             JSONObject record = records.getJSONObject(index);
-            fields(record, "id", "chat_id", "sender_id", "date", "text", "reply_markup", "edit_date", "entities", "rich_message");
+            fields(record, "id", "chat_id", "sender_id", "date", "text", "reply_markup", "edit_date", "entities", "rich_message", "photo", "caption", "caption_entities");
+            if (revisions != null) {
+                JSONObject revision = revisions.getJSONObject(index);
+                fields(revision, "chat_id", "message_id", "revision");
+                require(identifier(revision, "chat_id") == identifier(record, "chat_id")
+                        && identifier(revision, "message_id") == identifier(record, "id"));
+                identifier(revision, "revision");
+            }
             long chat = identifier(record, "chat_id");
             require(peersByChat.containsKey(chat));
             long bot = peersByChat.get(chat);
@@ -456,6 +493,21 @@ public final class GramLabBridge {
         }
     }
 
+    private static void dependencyUsers(JSONArray records, Snapshot state) throws JSONException {
+        Set<Long> seen = new HashSet<>();
+        for (int i = 0; i < records.length(); i++) {
+            JSONObject record = records.getJSONObject(i);
+            fields(record, "id", "is_bot", "first_name", "username", "language_code");
+            long id = identifier(record, "id");
+            require(seen.add(id));
+            TLRPC.User expected = null;
+            for (TLRPC.User user : state.dialogs.users) if (user.id == id) expected = user;
+            require(expected != null && record.get("is_bot") instanceof Boolean
+                    && expected.bot == record.getBoolean("is_bot")
+                    && expected.first_name.equals(record.getString("first_name")));
+        }
+    }
+
     private static long peerForChat(Snapshot state, long chat) {
         for (Map.Entry<Long, Long> entry : state.chatIdsByPeer.entrySet()) {
             if (entry.getValue() == chat) return entry.getKey();
@@ -478,8 +530,12 @@ public final class GramLabBridge {
 
     public static EventBatch events(JSONObject configuration, Snapshot state, long after, int limit) throws Exception {
         require(after >= 0 && after <= Integer.MAX_VALUE && limit > 0 && limit <= 1000);
-        JSONObject body = exchange(configuration, "/v2/changes?after=" + after + "&limit=" + limit, null);
-        fields(body, "schema", "world_id", "user_id", "cursor", "head", "now", "changes");
+        int version = configuration.has("bridge_version") ? integer(configuration, "bridge_version") : 2;
+        GramLabMedia.beginRequest(configuration);
+        JSONObject body = exchange(configuration, (version == 3 ? "/v3/changes" : "/v2/changes") + "?after=" + after + "&limit=" + limit, null);
+        fields(body, "schema", "world_id", "user_id", "cursor", "head", "now", "changes", "users", "assets");
+        if (version == 3) { dependencyUsers(body.getJSONArray("users"), state); GramLabMedia.install(body.getJSONArray("assets")); }
+        else require(!body.has("users") && !body.has("assets"));
         int cursor = integer(body, "cursor");
         int head = integer(body, "head");
         require(cursor >= after && cursor <= head);
@@ -489,7 +545,8 @@ public final class GramLabBridge {
         long previous = after;
         for (int index = 0; index < records.length(); index++) {
             JSONObject record = records.getJSONObject(index);
-            fields(record, "position", "type", "data", "request_id");
+            fields(record, "position", "type", "data", "request_id", "revision");
+            if (version == 3) identifier(record, "revision"); else require(!record.has("revision"));
             int position = integer(record, "position");
             require(position == previous + 1 && position <= cursor);
             previous = position;
@@ -637,15 +694,21 @@ public final class GramLabBridge {
         long chat = state.chatIdsByPeer.get(query.peer.user_id);
         JSONObject parameters = new JSONObject().put("request_id", UUID.randomUUID().toString())
                 .put("chat_id", chat).put("message_id", query.msg_id).put("data", data);
-        JSONObject body = exchange(configuration, "/v1/callbacks", parameters);
+        int version = configuration.has("bridge_version") ? integer(configuration, "bridge_version") : 2;
+        GramLabMedia.beginRequest(configuration);
+        String callbackPath = version == 3 ? "/v3/callbacks" : "/v1/callbacks";
+        JSONObject body = exchange(configuration, callbackPath, parameters);
         long deadline = SystemClock.elapsedRealtime() + 15000;
         String id = null;
         while (true) {
-            fields(body, "schema", "world_id", "user_id", "callback");
+            fields(body, "schema", "world_id", "user_id", "callback", "users", "assets", "message_revision");
+            if (version == 3) { dependencyUsers(body.getJSONArray("users"), state); GramLabMedia.install(body.getJSONArray("assets")); identifier(body, "message_revision"); }
+            else require(!body.has("users") && !body.has("assets") && !body.has("message_revision"));
             JSONObject record = body.getJSONObject("callback");
             fields(record, "id", "user_id", "chat_id", "message", "data", "chat_instance", "answer");
+            TLRPC.TL_message captured = message(record.getJSONObject("message"), state.persona, query.peer.user_id);
             String receivedId = record.getString("id");
-            require(receivedId.matches("[A-Za-z0-9_-]{1,128}") && (id == null || id.equals(receivedId)));
+            require(captured.id == query.msg_id && receivedId.matches("[A-Za-z0-9_-]{1,128}") && (id == null || id.equals(receivedId)));
             id = receivedId;
             require(identifier(record, "user_id") == state.persona && identifier(record, "chat_id") == chat
                     && identifier(record.getJSONObject("message"), "id") == query.msg_id
@@ -655,7 +718,7 @@ public final class GramLabBridge {
                 throw new IllegalStateException("GRAMLAB_CALLBACK_ANSWER_TIMEOUT");
             }
             Thread.sleep(100);
-            body = exchange(configuration, "/v1/callbacks/" + id, null);
+            body = exchange(configuration, callbackPath + "/" + id, null);
         }
     }
 }
diff --git a/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabMedia.java b/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabMedia.java
new file mode 100644
index 0000000..6812eb8
--- /dev/null
+++ b/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabMedia.java
@@ -0,0 +1,149 @@
+// SPDX-License-Identifier: GPL-2.0-or-later
+package org.telegram.gramlab;
+
+import org.json.JSONArray;
+import org.json.JSONException;
+import org.json.JSONObject;
+import org.telegram.tgnet.TLRPC;
+
+import java.io.File;
+import java.io.FileWriter;
+import org.telegram.messenger.ApplicationLoader;
+import java.util.LinkedHashMap;
+import java.util.Map;
+
+/** World-bound synthetic media descriptors; identifiers are never capabilities. */
+public final class GramLabMedia {
+    public static final class Asset {
+        public final long id;
+        public final String mimeType;
+        public final long size;
+        public final String sha256;
+        public final int width;
+        public final int height;
+        private Asset(long id, String mimeType, long size, String sha256, int width, int height) {
+            this.id = id; this.mimeType = mimeType; this.size = size; this.sha256 = sha256;
+            this.width = width; this.height = height;
+        }
+    }
+
+    private static String worldId;
+    private static JSONObject configuration;
+    private static long persona;
+    private static final Map<Long, Asset> assets = new LinkedHashMap<>();
+
+    private GramLabMedia() {}
+
+    public static synchronized void beginWorld(JSONObject config, String world) throws JSONException {
+        long configuredPersona = GramLabBridge.identifier(config, "user_id");
+        if (!world.equals(worldId) || persona != configuredPersona) {
+            assets.clear();
+            worldId = world;
+            persona = configuredPersona;
+        }
+        beginRequest(config);
+    }
+
+    public static synchronized void beginRequest(JSONObject config) throws JSONException {
+        int version = config.has("bridge_version") ? GramLabBridge.integer(config, "bridge_version") : 2;
+        if (version != 2 && version != 3) throw new IllegalArgumentException("GRAMLAB_BRIDGE_VERSION");
+        configuration = new JSONObject(config.toString());
+    }
+
+    public static synchronized void requireVersion3() {
+        if (configuration == null || configuration.optInt("bridge_version", 2) != 3)
+            throw new IllegalArgumentException("GRAMLAB_BRIDGE_MEDIA_REQUIRES_V3");
+    }
+
+    public static synchronized void install(JSONArray records) throws JSONException {
+        long previous = 0;
+        for (int i = 0; i < records.length(); i++) {
+            JSONObject record = records.getJSONObject(i);
+            GramLabBridge.fields(record, "asset_id", "mime_type", "file_size", "sha256", "width", "height");
+            long id = GramLabBridge.identifier(record, "asset_id");
+            long size = GramLabBridge.identifier(record, "file_size");
+            int width = GramLabBridge.integer(record, "width");
+            int height = GramLabBridge.integer(record, "height");
+            String mime = record.getString("mime_type");
+            String digest = record.getString("sha256");
+            if (id <= previous || size > 10_000_000 || width <= 0 || height <= 0
+                    || width + (long) height > 10_000 || width * (long) height > 25_000_000
+                    || Math.max(width, height) > 20L * Math.min(width, height)
+                    || !(mime.equals("image/png") || mime.equals("image/jpeg"))
+                    || !digest.matches("[0-9a-f]{64}")) throw new IllegalArgumentException("GRAMLAB_BRIDGE_INVALID_ASSET");
+            previous = id;
+            Asset incoming = new Asset(id, mime, size, digest, width, height);
+            Asset old = assets.get(id);
+            if (old != null && (!old.mimeType.equals(mime) || old.size != size || !old.sha256.equals(digest)
+                    || old.width != width || old.height != height)) throw new IllegalArgumentException("GRAMLAB_BRIDGE_ASSET_CHANGED");
+            assets.put(id, incoming);
+        }
+    }
+
+    public static synchronized Asset asset(long id) { return assets.get(id); }
+    public static synchronized JSONObject configuration() throws JSONException {
+        if (configuration == null) throw new IllegalStateException("GRAMLAB_BRIDGE_NOT_CONNECTED");
+        return new JSONObject(configuration.toString());
+    }
+
+    public static TLRPC.TL_photo photo(long assetId, int date) {
+        requireVersion3();
+        Asset asset = asset(assetId);
+        if (asset == null) throw new IllegalArgumentException("GRAMLAB_BRIDGE_UNKNOWN_ASSET");
+        TLRPC.TL_fileLocationToBeDeprecated location = new TLRPC.TL_fileLocationToBeDeprecated();
+        location.dc_id = 0; location.volume_id = asset.id; location.local_id = 1; location.secret = 0;
+        TLRPC.TL_photoSize size = new TLRPC.TL_photoSize();
+        size.type = "x"; size.location = location; size.w = asset.width; size.h = asset.height; size.size = (int) asset.size;
+        TLRPC.TL_photo photo = new TLRPC.TL_photo();
+        photo.id = asset.id; photo.access_hash = 0; photo.file_reference = new byte[0]; photo.date = date;
+        photo.dc_id = 0; photo.sizes.add(size);
+        return photo;
+    }
+
+    public static boolean matches(File file, Asset asset) {
+        try (java.io.FileInputStream input = new java.io.FileInputStream(file)) {
+            java.security.MessageDigest digest = java.security.MessageDigest.getInstance("SHA-256");
+            byte[] buffer = new byte[8192]; int count; long size = 0;
+            while ((count = input.read(buffer)) != -1) { size += count; digest.update(buffer, 0, count); }
+            return size == asset.size && hex(digest.digest()).equals(asset.sha256);
+        } catch (Exception error) { return false; }
+    }
+
+    public static String hex(byte[] bytes) {
+        StringBuilder result = new StringBuilder(bytes.length * 2);
+        for (byte value : bytes) result.append(String.format(java.util.Locale.ROOT, "%02x", value & 255));
+        return result.toString();
+    }
+
+    public static synchronized void trace(String event, long assetId, String fileName, long size, boolean digestOk) {
+        try {
+            File directory = new File(ApplicationLoader.applicationContext.getFilesDir(), "gramlab");
+            directory.mkdirs();
+            JSONObject row = new JSONObject().put("event", event).put("asset_id", assetId)
+                    .put("cache_file", fileName).put("file_size", size).put("digest_ok", digestOk);
+            try (FileWriter output = new FileWriter(new File(directory, "trace.jsonl"), true)) {
+                output.write(row.toString()); output.write("\n");
+            }
+        } catch (Exception ignored) {}
+    }
+
+    public static JSONObject describePhoto(TLRPC.Photo value) throws JSONException {
+        if (!(value instanceof TLRPC.TL_photo) || value.id <= 0 || value.dc_id != 0
+                || value.access_hash != 0 || value.file_reference == null || value.file_reference.length != 0
+                || value.sizes.size() != 1) throw new IllegalArgumentException("GRAMLAB_INVALID_SYNTHETIC_PHOTO");
+        TLRPC.PhotoSize size = value.sizes.get(0);
+        if (!(size instanceof TLRPC.TL_photoSize) || size.location == null || size.location.dc_id != 0
+                || size.location.volume_id != value.id || size.location.local_id != 1)
+            throw new IllegalArgumentException("GRAMLAB_INVALID_SYNTHETIC_PHOTO");
+        return new JSONObject().put("asset_id", value.id).put("dc_id", value.dc_id)
+                .put("access_hash", value.access_hash).put("file_reference_bytes", value.file_reference.length)
+                .put("size_type", size.type).put("volume_id", size.location.volume_id)
+                .put("local_id", size.location.local_id).put("width", size.w).put("height", size.h)
+                .put("file_size", size.size);
+    }
+
+    public static boolean isReserved(org.telegram.messenger.ImageLocation location) {
+        return location != null && location.location != null
+                && (location.dc_id == 0 || location.location.dc_id == 0);
+    }
+}
diff --git a/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabRichMessage.java b/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabRichMessage.java
index ae536ef..42c624e 100644
--- a/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabRichMessage.java
+++ b/TMessagesProj/src/main/java/org/telegram/gramlab/GramLabRichMessage.java
@@ -82,7 +82,9 @@ public final class GramLabRichMessage {
         fields(record, "blocks", "is_rtl");
         TL_iv.RichMessage result = new TL_iv.RichMessage();
         result.rtl = flag(record, "is_rtl");
-        result.blocks = blocks(record.getJSONArray("blocks"));
+        JSONArray sourceBlocks = record.getJSONArray("blocks");
+        result.blocks = blocks(sourceBlocks);
+        collectPhotos(sourceBlocks, result);
         return result;
     }
 
@@ -180,6 +182,26 @@ public final class GramLabRichMessage {
         return result;
     }
 
+    private static void collectPhotos(Object value, TL_iv.RichMessage result) throws JSONException {
+        if (value instanceof JSONArray) {
+            JSONArray array = (JSONArray) value;
+            for (int i = 0; i < array.length(); i++) collectPhotos(array.get(i), result);
+        } else if (value instanceof JSONObject) {
+            JSONObject object = (JSONObject) value;
+            if ("photo".equals(object.optString("type"))) {
+                long id = GramLabBridge.identifier(object, "asset_id");
+                boolean present = false;
+                for (org.telegram.tgnet.TLRPC.Photo photo : result.photos) present |= photo.id == id;
+                if (!present) result.photos.add(GramLabMedia.photo(id, 0));
+            }
+            Iterator<String> keys = object.keys();
+            while (keys.hasNext()) {
+                String key = keys.next();
+                if (!"asset_id".equals(key)) collectPhotos(object.get(key), result);
+            }
+        }
+    }
+
     private static TL_iv.RichText text(Object value) throws JSONException {
         if (value instanceof String) {
             TL_iv.textPlain result = new TL_iv.textPlain();
@@ -290,6 +312,22 @@ public final class GramLabRichMessage {
                 pre.language = record.has("language") ? string(record, "language") : "";
                 return pre;
             }
+            case "photo": {
+                fields(record, "type", "asset_id", "caption");
+                TL_iv.pageBlockPhoto photo = new TL_iv.pageBlockPhoto();
+                photo.photo_id = GramLabBridge.identifier(record, "asset_id");
+                photo.caption = new TL_iv.PageCaption();
+                photo.caption.text = new TL_iv.textEmpty();
+                photo.caption.credit = new TL_iv.textEmpty();
+                if (record.has("caption")) {
+                    JSONObject caption = record.getJSONObject("caption");
+                    fields(caption, "text", "credit");
+                    require(caption.has("text"));
+                    photo.caption.text = text(caption.get("text"));
+                    photo.caption.credit = optionalText(caption, "credit");
+                }
+                return photo;
+            }
             case "divider":
                 fields(record, "type");
                 return new TL_iv.pageBlockDivider();
diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/FileLoader.java b/TMessagesProj/src/main/java/org/telegram/messenger/FileLoader.java
index 87465e9..82c7932 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/FileLoader.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/FileLoader.java
@@ -8,6 +8,9 @@
 
 package org.telegram.messenger;
 
+import org.telegram.gramlab.GramLabMedia;
+import org.json.JSONObject;
+
 import android.text.TextUtils;
 import android.util.SparseArray;
 
@@ -215,6 +218,10 @@ public class FileLoader extends BaseController {
 
 
     private final ConcurrentHashMap<String, FileLoadOperation> loadOperationPaths = new ConcurrentHashMap<>();
+    // All synthetic registration, cache publication and notifications use this one lock.
+    private final Object syntheticPhotoLock = new Object();
+    private final ConcurrentHashMap<String, SyntheticPhotoLoad> syntheticPhotoLoads = new ConcurrentHashMap<>();
+    private final java.util.concurrent.ExecutorService syntheticPhotoExecutor = java.util.concurrent.Executors.newFixedThreadPool(2);
     private final ConcurrentHashMap<String, LoadOperationUIObject> loadOperationPathsUI = new ConcurrentHashMap<>(10, 1, 2);
     private final HashMap<String, Long> uploadSizes = new HashMap<>();
 
@@ -604,7 +611,23 @@ public class FileLoader extends BaseController {
         } else {
             fileName = name;
         }
-        LoadOperationUIObject uiObject = loadOperationPathsUI.remove(fileName);
+        SyntheticPhotoLoad synthetic;
+        java.net.HttpURLConnection syntheticConnection = null;
+        LoadOperationUIObject uiObject;
+        synchronized (syntheticPhotoLock) {
+            synthetic = syntheticPhotoLoads.get(fileName);
+            if (synthetic != null) {
+                syntheticConnection = synthetic.cancelLocked(deleteFile);
+                uiObject = null;
+            } else {
+                uiObject = loadOperationPathsUI.remove(fileName);
+            }
+        }
+        if (synthetic != null) {
+            // Closing a blocked connection must not hold the lifecycle lock.
+            if (syntheticConnection != null) syntheticConnection.disconnect();
+            return;
+        }
         Runnable runnable = uiObject != null ? uiObject.loadInternalRunnable : null;
         boolean removed = uiObject != null;
         if (runnable != null) {
@@ -757,6 +780,164 @@ public class FileLoader extends BaseController {
         }
     }
 
+    private final class SyntheticPhotoLoad implements Runnable {
+        final String fileName;
+        final GramLabMedia.Asset asset;
+        final JSONObject configuration;
+        final Object parentObject;
+        final File temporary;
+        final File target;
+        final LoadOperationUIObject uiObject = new LoadOperationUIObject();
+        // Guarded exclusively by syntheticPhotoLock; no per-attempt monitor.
+        private boolean canceled;
+        private boolean terminal;
+        private java.net.HttpURLConnection connection;
+
+        SyntheticPhotoLoad(String fileName, GramLabMedia.Asset asset, JSONObject configuration,
+                Object parentObject, File target) {
+            this.fileName = fileName; this.asset = asset; this.configuration = configuration;
+            this.parentObject = parentObject; this.target = target;
+            this.temporary = new File(target.getParentFile(), fileName + ".gramlab-" + java.util.UUID.randomUUID() + ".part");
+        }
+
+        private boolean attach(java.net.HttpURLConnection value) {
+            synchronized (syntheticPhotoLock) {
+                if (terminal || canceled) return false;
+                connection = value;
+                return true;
+            }
+        }
+
+        // Caller holds syntheticPhotoLock and closes the returned connection after unlocking.
+        private java.net.HttpURLConnection cancelLocked(boolean deleteFile) {
+            if (!terminal) {
+                canceled = true;
+                temporary.delete();
+                if (deleteFile) target.delete();
+                finishLocked(1, "media_load_cancel");
+            }
+            return connection;
+        }
+
+        // Caller holds syntheticPhotoLock. Enqueue the terminal notification before
+        // releasing filename ownership, so a retry cannot precede that notification.
+        private void finishLocked(int reason, String event) {
+            if (terminal) return;
+            terminal = true;
+            GramLabMedia.trace(event, asset.id, fileName, asset.size, reason == -1);
+            try {
+                if (delegate != null) {
+                    if (reason == -1) delegate.fileDidLoaded(fileName, target, parentObject, MEDIA_DIR_IMAGE);
+                    else delegate.fileDidFailedLoad(fileName, reason);
+                }
+            } finally {
+                loadOperationPathsUI.remove(fileName, uiObject);
+                syntheticPhotoLoads.remove(fileName, this);
+            }
+        }
+
+        private void fail() {
+            synchronized (syntheticPhotoLock) {
+                finishLocked(canceled ? 1 : 0, canceled ? "media_load_cancel" : "media_load_failure");
+            }
+        }
+
+        @Override public void run() {
+            java.net.HttpURLConnection active = null;
+            try {
+                java.security.MessageDigest digest = java.security.MessageDigest.getInstance("SHA-256");
+                java.net.URL endpoint = new java.net.URL(configuration.getString("endpoint"));
+                java.net.URL targetUrl = new java.net.URL(endpoint, "/v3/assets/" + asset.id);
+                active = (java.net.HttpURLConnection) targetUrl.openConnection(java.net.Proxy.NO_PROXY);
+                active.setInstanceFollowRedirects(false); active.setConnectTimeout(3000); active.setReadTimeout(5000);
+                active.setUseCaches(false); active.setRequestProperty("Authorization", "Bearer " + configuration.getString("capability"));
+                active.setRequestProperty("Accept", asset.mimeType); active.setRequestProperty("Connection", "close");
+                if (!attach(active)) return;
+                int status = active.getResponseCode();
+                if (status != 200 || active.getContentLengthLong() != asset.size
+                        || !asset.mimeType.equals(active.getContentType())) throw new java.io.IOException("asset_response");
+                long count = 0; byte[] buffer = new byte[8192];
+                try (java.io.InputStream input = active.getInputStream()) {
+                    java.io.FileOutputStream stream;
+                    synchronized (syntheticPhotoLock) {
+                        if (terminal || canceled) return;
+                        stream = new java.io.FileOutputStream(temporary);
+                    }
+                    try (java.io.FileOutputStream output = stream) {
+                        int read;
+                        while ((read = input.read(buffer)) != -1) {
+                            synchronized (syntheticPhotoLock) {
+                                if (terminal || canceled) return;
+                                if (count + read > asset.size) throw new java.io.IOException("asset_oversized");
+                                output.write(buffer, 0, read); digest.update(buffer, 0, read); count += read;
+                                if (delegate != null) delegate.fileLoadProgressChanged(null, fileName, count, asset.size);
+                            }
+                        }
+                        output.getFD().sync();
+                    }
+                }
+                if (count != asset.size || !GramLabMedia.hex(digest.digest()).equals(asset.sha256))
+                    throw new java.io.IOException("asset_integrity");
+                synchronized (syntheticPhotoLock) {
+                    if (terminal || canceled) return;
+                    java.nio.file.Files.move(temporary.toPath(), target.toPath(),
+                            java.nio.file.StandardCopyOption.ATOMIC_MOVE,
+                            java.nio.file.StandardCopyOption.REPLACE_EXISTING);
+                    finishLocked(-1, "media_load_success");
+                }
+            } catch (Exception error) {
+                fail();
+            } finally {
+                if (active != null) active.disconnect();
+                temporary.delete();
+            }
+        }
+    }
+
+    private void loadSyntheticPhoto(ImageLocation location, Object parentObject, String ext, int cacheType) {
+        String fileName = getAttachFileName(location.location, ext);
+        synchronized (syntheticPhotoLock) {
+            // Coalesce before inspecting or deleting a target another attempt may publish.
+            SyntheticPhotoLoad existing = syntheticPhotoLoads.get(fileName);
+            if (existing != null) {
+                GramLabMedia.trace("media_load_coalesced", existing.asset.id, fileName, existing.asset.size, false);
+                return;
+            }
+            GramLabMedia.Asset asset = location.location.volume_id > 0 && location.location.local_id == 1
+                    ? GramLabMedia.asset(location.location.volume_id) : null;
+            if (asset == null) {
+                GramLabMedia.trace("media_load_failure", location.location.volume_id, fileName, 0, false);
+                if (delegate != null) delegate.fileDidFailedLoad(fileName, 0);
+                return;
+            }
+            File directory = getDirectory(cacheType == 0 ? MEDIA_DIR_IMAGE : MEDIA_DIR_CACHE);
+            File target = new File(directory, fileName);
+            if (target.isFile() && target.length() == asset.size && GramLabMedia.matches(target, asset)) {
+                GramLabMedia.trace("media_cache_hit", asset.id, fileName, asset.size, true);
+                if (delegate != null) delegate.fileDidLoaded(fileName, target, parentObject, MEDIA_DIR_IMAGE);
+                return;
+            }
+            if (target.exists() && !target.delete()) {
+                GramLabMedia.trace("media_load_failure", asset.id, fileName, asset.size, false);
+                if (delegate != null) delegate.fileDidFailedLoad(fileName, 0);
+                return;
+            }
+            JSONObject configuration;
+            try { configuration = GramLabMedia.configuration(); }
+            catch (Exception error) {
+                GramLabMedia.trace("media_load_failure", asset.id, fileName, asset.size, false);
+                if (delegate != null) delegate.fileDidFailedLoad(fileName, 0);
+                return;
+            }
+            SyntheticPhotoLoad load = new SyntheticPhotoLoad(fileName, asset, configuration, parentObject, target);
+            syntheticPhotoLoads.put(fileName, load);
+            loadOperationPathsUI.put(fileName, load.uiObject);
+            GramLabMedia.trace("media_load_start", asset.id, fileName, asset.size, false);
+            try { syntheticPhotoExecutor.execute(load); }
+            catch (RuntimeException error) { load.fail(); }
+        }
+    }
+
     public File getLocalFile(ImageLocation imageLocation) {
         if (imageLocation == null) return null;
         String fileName;
@@ -791,6 +972,10 @@ public class FileLoader extends BaseController {
         if (imageLocation == null) {
             return;
         }
+        if (GramLabMedia.isReserved(imageLocation)) {
+            loadSyntheticPhoto(imageLocation, parentObject, ext, cacheType);
+            return;
+        }
         if (cacheType == 0 && (imageLocation.isEncrypted() || imageLocation.photoSize != null && imageLocation.getSize() == 0)) {
             cacheType = 1;
         }
diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/ImageLoader.java b/TMessagesProj/src/main/java/org/telegram/messenger/ImageLoader.java
index 73815bc..843061f 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/ImageLoader.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/ImageLoader.java
@@ -132,6 +132,7 @@ public class ImageLoader {
     private DispatchQueue imageLoadQueue = new DispatchQueue("imageLoadQueue");
     private HashMap<String, String> replacedBitmaps = new HashMap<>();
     private ConcurrentHashMap<String, long[]> fileProgresses = new ConcurrentHashMap<>();
+    private ConcurrentHashMap<String, Long> syntheticProgressTimes = new ConcurrentHashMap<>();
     private HashMap<String, ThumbGenerateTask> thumbGenerateTasks = new HashMap<>();
     private HashMap<String, Integer> forceLoadingImages = new HashMap<>();
     private static ThreadLocal<byte[]> bytesLocal = new ThreadLocal<>();
@@ -2188,6 +2189,7 @@ public class ImageLoader {
                 @Override
                 public void fileDidLoaded(final String location, final File finalFile, Object parentObject, final int type) {
                     fileProgresses.remove(location);
+                    syntheticProgressTimes.remove(location);
                     AndroidUtilities.runOnUIThread(() -> {
                         if (finalFile != null && (location.endsWith(".mp4") || location.endsWith(".jpg"))) {
                             FilePathDatabase.FileMeta meta = FileLoader.getFileMetadataFromParent(currentAccount, parentObject);
@@ -2220,6 +2222,7 @@ public class ImageLoader {
                 @Override
                 public void fileDidFailedLoad(final String location, final int canceled) {
                     fileProgresses.remove(location);
+                    syntheticProgressTimes.remove(location);
                     AndroidUtilities.runOnUIThread(() -> {
                         ImageLoader.this.fileDidFailedLoad(location, canceled);
                         NotificationCenter.getInstance(currentAccount).postNotificationName(NotificationCenter.fileLoadFailed, location, canceled);
@@ -2230,7 +2233,7 @@ public class ImageLoader {
                 public void fileLoadProgressChanged(FileLoadOperation operation, final String location, long uploadedSize, long totalSize) {
                     fileProgresses.put(location, new long[]{uploadedSize, totalSize});
 
-                    if (!imageLoadingByUrlPframe.isEmpty() && operation.checkPrefixPreloadFinished()) {
+                    if (operation != null && !imageLoadingByUrlPframe.isEmpty() && operation.checkPrefixPreloadFinished()) {
                         imageLoadQueue.postRunnable(() -> {
                             CacheImage img = imageLoadingByUrlPframe.remove(location);
                             if (img == null) {
@@ -2279,9 +2282,14 @@ public class ImageLoader {
                         });
                     }
                     long currentTime = SystemClock.elapsedRealtime();
-                    if (operation.lastProgressUpdateTime == 0 || operation.lastProgressUpdateTime < currentTime - 500 || uploadedSize == 0) {
-                        operation.lastProgressUpdateTime = currentTime;
-
+                    Long syntheticTime = operation == null ? syntheticProgressTimes.get(location) : null;
+                    boolean notify = operation != null
+                            ? operation.lastProgressUpdateTime == 0 || operation.lastProgressUpdateTime < currentTime - 500 || uploadedSize == 0
+                            : syntheticTime == null || syntheticTime < currentTime - 500 || uploadedSize == totalSize;
+                    if (notify) {
+                        if (operation != null) operation.lastProgressUpdateTime = currentTime;
+                        else if (uploadedSize == totalSize) syntheticProgressTimes.remove(location);
+                        else syntheticProgressTimes.put(location, currentTime);
                         AndroidUtilities.runOnUIThread(() -> NotificationCenter.getInstance(currentAccount).postNotificationName(NotificationCenter.fileLoadProgressChanged, location, uploadedSize, totalSize));
                     }
                 }
diff --git a/TMessagesProj_GramLab/src/main/java/org/telegram/gramlab/BridgeProbe.java b/TMessagesProj_GramLab/src/main/java/org/telegram/gramlab/BridgeProbe.java
index 93d3014..2ad5c4c 100644
--- a/TMessagesProj_GramLab/src/main/java/org/telegram/gramlab/BridgeProbe.java
+++ b/TMessagesProj_GramLab/src/main/java/org/telegram/gramlab/BridgeProbe.java
@@ -162,6 +162,24 @@ public final class BridgeProbe {
             if (!language.isEmpty()) result.put("language", language);
             return result;
         }
+        if (block instanceof TL_iv.pageBlockPhoto) {
+            TL_iv.pageBlockPhoto photo = (TL_iv.pageBlockPhoto) block;
+            if (photo.flags != 0 || photo.spoiler || photo.url != null || photo.webpage_id != 0)
+                throw new IllegalStateException("GRAMLAB_PROBE_UNEXPECTED_PHOTO_FLAGS");
+            result.put("type", "photo").put("asset_id", photo.photo_id);
+            boolean text = photo.caption != null && photo.caption.text != null
+                    && !(photo.caption.text instanceof TL_iv.textEmpty)
+                    && !(photo.caption.text instanceof TL_iv.textPlain && ((TL_iv.textPlain) photo.caption.text).text.isEmpty());
+            boolean credit = photo.caption != null && photo.caption.credit != null
+                    && !(photo.caption.credit instanceof TL_iv.textEmpty)
+                    && !(photo.caption.credit instanceof TL_iv.textPlain && ((TL_iv.textPlain) photo.caption.credit).text.isEmpty());
+            if (text || credit) {
+                JSONObject caption = new JSONObject().put("text", richText(photo.caption.text));
+                if (credit) caption.put("credit", richText(photo.caption.credit));
+                result.put("caption", caption);
+            }
+            return result;
+        }
         if (block instanceof TL_iv.pageBlockDivider) return result.put("type", "divider");
         if (block instanceof TL_iv.pageBlockBlockquoteBlocks) {
             TL_iv.pageBlockBlockquoteBlocks quote = (TL_iv.pageBlockBlockquoteBlocks) block;
@@ -255,10 +273,15 @@ public final class BridgeProbe {
     }
 
     private static JSONObject richMessage(TL_iv.RichMessage value) throws Exception {
-        if (value.part || !value.photos.isEmpty() || !value.documents.isEmpty()) {
+        if (value.part || !value.documents.isEmpty()) {
             throw new IllegalStateException("GRAMLAB_PROBE_UNEXPECTED_RICH_MEDIA");
         }
         JSONObject result = new JSONObject().put("blocks", richBlocks(value.blocks));
+        if (!value.photos.isEmpty()) {
+            JSONArray photos = new JSONArray();
+            for (TLRPC.Photo photo : value.photos) photos.put(GramLabMedia.describePhoto(photo));
+            result.put("native_photos", photos);
+        }
         if (value.rtl) result.put("is_rtl", true);
         return result;
     }
@@ -319,6 +342,9 @@ public final class BridgeProbe {
                 if (message.rich_message != null) {
                     observed.put("rich_message", richMessage(message.rich_message));
                 }
+                if (message.media instanceof TLRPC.TL_messageMediaPhoto) {
+                    observed.put("native_photo", GramLabMedia.describePhoto(message.media.photo));
+                }
                 messages.put(observed);
             }
         }
