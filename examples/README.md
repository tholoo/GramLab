# Examples

Reserved for runnable consumer scenarios and labeled synthetic conversation examples generated
from passing tests. No runnable examples exist yet. Use a minimal framework-independent bot
before integrating consumer application so the core does not inherit one consumer's assumptions.
