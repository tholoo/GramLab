# Keep the independent core separate from client-derived code

Write the Python simulator/SDK independently under MIT and retain applicable upstream copyleft
terms in Android-derived components. This preserves the reusable-library goal while allowing real
client rendering; schema/code/asset provenance and actual distribution obligations still require
review, rather than treating a directory split as proof of license independence.
